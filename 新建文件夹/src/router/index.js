import Vue from 'vue'
import VueRouter from 'vue-router'

Vue.use(VueRouter)

const routes = [
    {
        //path: '/entrance',
        //name: '入口',
        //component: Entrance
    },
]

export default new VueRouter({
    routes
})
